---
# Host vars for optiplex

# UFW extra rules — ports needed by stacks on this host
ufw_extra_rules:
  # Beszel agent
  - { port: "45876", proto: tcp, src: "192.168.1.0/24" }
  # Beszel hub web UI
  - { port: "8090", proto: tcp, src: "192.168.1.0/24" }
