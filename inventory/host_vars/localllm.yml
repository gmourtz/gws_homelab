---
# Host vars for localllm (VLAN 10 — Trusted, local LLM serving)
# Hardware: Dell OptiPlex Micro Plus 7010 (i7-13700T, 16GB DDR5, 256GB NVMe)

# UFW extra rules
ufw_extra_rules:
  - { port: "11434", proto: tcp, src: "192.168.10.0/24" }   # Ollama API (whole trusted VLAN)
  - { port: "8080",  proto: tcp, src: "192.168.10.20"   }   # Open WebUI (Caddy on optiplex only)
  - { port: "45876", proto: tcp, src: "192.168.10.0/24" }   # Beszel agent (VLAN-wide, matches rpi3/openclaw pattern)
  - { port: "7007",  proto: tcp, src: "192.168.10.20"   }   # Dozzle agent
  - { port: "2375",  proto: tcp, src: "192.168.10.20"   }   # Docker socket proxy (dozzle aggregator)

# Tailscale enabled — same pattern as optiplex/rpi3 for remote access
tailscale_enabled: true
