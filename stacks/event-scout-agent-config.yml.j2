# Managed by Ansible — do not edit on the host.
# Source of truth: `event_scout_agent` in inventory/group_vars/all/main.yml
# Apply changes with `make stacks`.
{{ event_scout_agent | to_nice_yaml(indent=2) }}
